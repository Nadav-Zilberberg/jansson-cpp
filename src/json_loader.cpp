#include "json_loader.hpp"

namespace jasson {

// Implementation matches the simple interface in json_loader.hpp
// No additional implementation needed as everything is inline in the header

} // namespace jasson
