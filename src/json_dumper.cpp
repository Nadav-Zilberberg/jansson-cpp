#include "json_dumper.hpp"
#include <fstream>
#include <stdexcept>

namespace jasson {

// Implementation matches the simple interface in json_dumper.hpp
// No additional implementation needed as everything is inline in the header

} // namespace jasson
