#include "json_types.hpp"
#include "json_value.hpp"

namespace jasson {

// Implementation matches the simple interface in json_value.hpp
// No additional implementation needed as everything is inline in the header

} // namespace jasson
